export const API_BASE_URL: string = import.meta.env.BASE_URL // i take the base url for all the configuration
export const API_PREFIX: string = '/api'

//full api URL
export const API_URL = `${API_BASE_URL}${API_PREFIX}`